name: Continuous Integration

on:
  pull_request:
    branches:
      - main

jobs:
  merge_pull_request:
    runs-on: ubuntu-latest
    steps:
      - name: Set outputs
        id: vars
        run: echo "SHORT_SHA=`echo ${{ github.sha }}`" >> $GITHUB_ENV

      - name: Check outputs
        run: echo ${{ steps.vars.outputs }}

      - name: Checkout main branch
        uses: actions/checkout@v2

      # - name: Build image
      #   run: docker compose -f docker-compose.yml build

      # - name: Install doctl
      #   uses: digitalocean/action-doctl@v2
      #   with:
      #     token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      # - name: Tag image 
      #   run: docker tag herpit/ingestor registry.digitalocean.com/herpit-reg/herpit-ingestor:1.0.0

      # - name: Log in to DO Container Registry
      #   run: doctl registry login --expiry-seconds 600

      # - name: Push image to DO Container Registry
      #   run: docker push registry.digitalocean.com/herpit-reg/herpit-ingestor:1.0.0